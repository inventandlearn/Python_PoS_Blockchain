apiVersion: v1
kind: Service
metadata:
  name: nodeapiconnections
  namespace: posblockchain
spec:
  selector:
    app: posblockmain-pod
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
